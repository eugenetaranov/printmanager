# Managed by Tack — roles/scan-server (escl backend). Use only the sane-airscan
# (eSCL/WSD) backend, so `scanimage` reaches the driverless scanner directly and
# doesn't probe every other SANE backend.
airscan
